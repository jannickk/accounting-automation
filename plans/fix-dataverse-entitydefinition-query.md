# Plan

- Inspect the Dataverse utility code that queries `EntityDefinitions` after creation.
- Change the query builder to use the singular Dataverse table schema name required by the SDK.
- Validate the updated Python file for syntax errors.
